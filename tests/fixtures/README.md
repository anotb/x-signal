# Synthetic parser fixtures

Every payload in this directory is hand-authored synthetic data shaped like the corresponding X operation. It contains no captured post text, post ID, author, account profile, feed, bookmark, cookie, or live response.

Keep new fixtures synthetic. Reduce a captured failure to the smallest invented structure needed for the regression before committing it.
